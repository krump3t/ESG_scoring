services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: esg-scoring:ci
    ports:
      - "8000:8000"
    environment:
      PYTHONHASHSEED: "0"
      SEED: "42"
      LIVE_EMBEDDINGS: "false"
      ALLOW_NETWORK: "false"
    volumes:
      - ./artifacts:/app/artifacts
